version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck

formatters:
  settings:
    goimports:
      local-prefixes:
        - code
  enable:
    - goimports
